package com.Hayati.Reservation.des.Hotels.services;
import org.springframework.stereotype.Service;

@Service
public class UserService {

    // Your logic to handle user operations, like changing passwords
    public boolean changePassword(String email, String newPassword) {
        // Logic to update password in the database
        return true;  // Return true if password is successfully changed
    }
}
